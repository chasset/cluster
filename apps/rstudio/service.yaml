apiVersion: v1
kind: Service
metadata:
  name: rstudio-service
  namespace: rstudio
spec:
  selector:
    app: rstudio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8787
  type: ClusterIP
