apiVersion: v1
kind: Service
metadata:
  name: rstudio-service
  namespace: rstudio
  annotations:
    tailscale.com/expose: 'true'
    tailscale.com/hostname: rstudio
spec:
  selector:
    app: rstudio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8787
  type: ClusterIP
