apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: debian-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block-ec
  resources:
    requests:
      storage: 10Ti
