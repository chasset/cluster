# code: language=ansible
- name: Install kubernetes on control plane nodes
  become: true
  become_method: ansible.builtin.sudo
  block:
    - name: Install kubectl
      ansible.builtin.apt:
        name: kubectl
        state: present

    - name: Mark packages to not upgrade
      ansible.builtin.command: apt-mark hold kubectl
